import Link from 'next/link';

const Infos = () => {
    return (
        <header className='text-center bg-blue-500 p-2 text-2xl'>
            Infos
        </header>
    );
};

export default Infos;